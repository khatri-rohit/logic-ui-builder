interface:
  display_name: "Logic Ui Builder"
  short_description: "Repo-specific patterns and workflows for logic-ui-builder"
  default_prompt: "Use the logic-ui-builder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true